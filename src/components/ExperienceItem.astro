---
import LinkInline from "./LinkInline.astro"
import Tag from "./Tag.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
  tags: string[]
}

const { title, company, description, link, date, tags } = Astro.props
---

<div
  class="relative mx-12 pb-12 grid before:absolute before:left-[-35px] before:block before:h-full before:border-l-2 before:border-black/20 dark:before:border-white/15 before:content-[''] md:grid-cols-5 md:gap-10 md:space-x-4]"
>
  <div class="relative pb-12 md:col-span-2">
    <div class="sticky top-0">
      <span class="text-cyan-400 -left-[44px] absolute rounded-full text-5xl "
        >&bull;</span
      >

      <h3 class="text-xl font-bold text-cyan-400">{title}</h3>
      <h4 class="font-semibold text-l  dark:text-white">{company}</h4>
      <time class="p-0 m-0 text-sm dark:text-slate-300/90">{date}</time>
    </div>
  </div>
  <div class="relative flex flex-col gap-2 pb-4 text-slate-300 md:col-span-3">
    {description}
    <ul class="flex flex-row flex-wrap mb-2 gap-2">
      {tags.map((tag) => (
        <Tag>{tag}</Tag>
      ))}
    </ul>
    {
      link && (
        <LinkInline href={link}>
          Saber más{" "}
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="w-5 icon icon-tabler icon-tabler-chevron-right"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            stroke-width="2"
            stroke="currentColor"
            fill="none"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <>
              <path stroke="none" d="M0 0h24v24H0z" fill="none" />
              <path d="M9 6l6 6l-6 6" />
            </>
          </svg>
        </LinkInline>
      )
    }
  </div>
</div>
