<div class="flex items-center">
  <span class="relative inline-flex overflow-hidden rounded-full p-[3px]">
    <span
      class="absolute inset-[-10000%] animate-[spin_2s_linear_infinite] bg-[conic-gradient(from_90deg_at_50%_50%,#00A0B4_0%,#B2EBF2_50%,#00A0B4_100%)] opacity-75 blur group-hover:opacity-100"
    ></span>
    <div class="card-content inline-flex w-full cursor-default items-center justify-center whitespace-nowrap rounded-full px-3 py-1 text-sm text-[rgb(var(--color-text))] backdrop-blur-3xl">
      <slot />
    </div>
  </span>
</div>

<style>
  .card-content {
    background: linear-gradient(
      180deg,
      rgba(var(--color-surface), 0.95) 0%,
      rgba(var(--color-surface-muted), 0.92) 100%
    );
    border: 1px solid rgba(var(--color-border), 0.18);
  }
</style>
